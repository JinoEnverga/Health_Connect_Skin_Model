# Ignore the dataset (large; retrieve/regenerate separately)
skin-disease-datasaet/

# Ignore the full-precision source model — only the quantized
# best_model_final.tflite is needed for deployment
best_model_final.keras

# Ignore the training script (cloud deployment only needs the API)
train_model.py

# Ignore Python cache and virtual environments
__pycache__/
venv/
.env
